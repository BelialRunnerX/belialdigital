import { StudioDesk } from "@/components/studio-desk";

export const metadata = { title: "Studio — Belial Digital" };
export const dynamic = "force-dynamic";

export default function StudioPage() {
  return <StudioDesk />;
}
